name: Claude PR Review

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]
    paths-ignore:
      - "*.md"
      - "docs/**"
      - "LICENSE"
      - "signatures/**"

concurrency:
  group: claude-pr-review-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  review:
    if: >-
      ${{
        !github.event.pull_request.draft &&
        github.actor != 'dependabot[bot]' &&
        github.event.pull_request.user.login != 'dependabot[bot]'
      }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      pull-requests: write
      id-token: write
      actions: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Fetch PR discussion context
        env:
          GH_TOKEN: ${{ github.token }}
          REPO_OWNER: ${{ github.repository_owner }}
          REPO_NAME: ${{ github.event.repository.name }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: bash .github/scripts/fetch-pr-comments.sh

      - name: Build review prompt
        env:
          PROJECT_NAME: "RipperMod Manager — Cyberpunk 2077 mod manager (FastAPI + React/Tauri)"
          REPO_SLUG: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          EVENT_ACTION: ${{ github.event.action }}
        run: |
          {
            echo 'REVIEW_PROMPT<<PROMPT_EOF'
            echo '<pr_context>'
            echo "REPO: ${REPO_SLUG}"
            echo "PROJECT: ${PROJECT_NAME}"
            echo "PR NUMBER: ${PR_NUMBER}"
            echo "EVENT: ${EVENT_ACTION}"
            echo '</pr_context>'
            echo '<existing_discussions>'
            cat /tmp/pr_comments_context.txt 2>/dev/null || echo 'No existing discussions.'
            echo '</existing_discussions>'
            echo '<review_instructions>'
            cat .github/prompts/pr-review.prompt.md
            echo '</review_instructions>'
            echo 'PROMPT_EOF'
          } >> "$GITHUB_ENV"

      - name: Claude PR Review
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          track_progress: true
          include_fix_links: true
          prompt: ${{ env.REVIEW_PROMPT }}
          claude_args: |
            --model claude-opus-4-6
            --verbose
            --allowedTools "mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*),Bash(git fetch:*),Bash(git diff:*),Bash(git log:*),Bash(gh pr review:*),Bash(gh api:*)"
